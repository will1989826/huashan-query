# 更新日志（Changelog）

本项目版本号遵循**语义化版本 SemVer**：`主版本.次版本.修订号`
- **主版本**：不兼容的大改（如整体重构、数据/用法变化）。
- **次版本**：新增功能，向后兼容（如新增筛选、新页面）。
- **修订号**：修 bug / 小改，向后兼容。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。最新版本在最上面。
分类用：`新增` / `优化` / `修复` / `变更` / `移除`。

发布流程：
1. 平时把改动记到下面的 `[未发布]`。
2. 发布时：改根目录 `VERSION` 为新版本号，把 `[未发布]` 改名为 `[x.y.z] - 日期`，再新建一个空的 `[未发布]`。
3. 双击 `build.bat` → 生成 `bin\华山战力查询-vx.y.z.exe`，把它发出去。

---

## [未发布]
### 计划
-

## [0.2.1] - 2026-08-24
### 新增
- **按 ID 精确查找**：搜索框上方显式切换「按名字 / 按 ID」，不再用“纯数字=ID”的隐式猜测。
- **检查更新**：⚙ 菜单新增「检查更新」，从更新清单比对版本并给出下载入口（清单地址构建时经 `-ldflags` 注入，不写死在代码里）。
- **更新了什么**：⚙ 菜单新增，用大白话列出各版本变化。

### 优化
- 姓名搜索按相关度排序（完全相同 > 前缀 > 包含 > 其余），最相关者置顶；并自动预热其全量数据（含按身份/逐场），点开单人详情或加入对比更快；测试版关闭预热以省额度。
- 引导页 / 过期提示补充：微信登录后需先关掉战力页、令牌才会写入本地，再重新检测（否则刚登录仍读到旧令牌）。

### 变更
- 移除「分享给朋友」（并入「更新了什么」）。

## [0.2.0] - 2026-08-23
### 新增
- **多人对比**（对比篮，最多 12 人）：搜索结果一键「＋ 对比」加入；支持输入选手 **ID 直达**（重名时靠结果里的 `#编号` 逐个挑准加入）。
  - **按阵营**：综合 / 好人 / 狼人，点列头排序。
  - **按身份**：`人 × 身份`矩阵（选一个指标，一屏看谁擅长哪个身份）/ `单个身份`（选一个身份比多项指标）；均可点列头排序。
  - 全体共享赛区 / 赛季范围；可勾选行聚焦子集；点选手名下钻单人详情。
  - 「按身份」的逐场数据在后台预热，切到该页时命中本地缓存秒显。
- **分享给朋友**：⚙ 菜单一键复制介绍语（自带作者署名）。
- ⚙ 菜单显示**版本号**；页脚显示作者署名。

### 优化
- 文案更直白：`按阵营 / 按身份`、`对局`、`复盘（阵容 · 投票 · 技能）`等（替换原“浅层 / 深层 / 牌局 / 刀验”等黑话）。
- 单人详情与对比表共用同一“当前视图”，异步请求不再互相覆盖 `#detail`。
- 提取共享渲染原语（键值取值 / 列排序），单人详情表、角色表与对比表复用，降低耦合。

### 修复
- 对比切换赛区 / 赛季后，不再短暂显示上一个作用域的旧数据。
- 移出 / 清空对比篮后，搜索结果里的「＋ 对比」按钮恢复可用（腾出空位后“已满”也会复原）。
- 「按身份」数据加载失败 / 为空 / 部分失败均有明确状态，不再永久停在“加载中”或把失败误报成“暂无”。
- 测试版额度：同一名选手全程只计一次查询（含 ID 直达、换赛区 / 赛季、重复打开）。

## [0.0.1] - 2026-08-22
首个发布版本。

### 功能
- 选手搜索：按选手名搜索，展示头像 / 门派 / 总分。
- 作用域筛选：赛区 / 赛季 / 门派三级筛选，候选项互相联动。
- 数据面板：综合 / 好人局 / 狼人局聚合指标 + 角色表现表（可按场次 / 场均分 / 胜率 / MVP / 尽力 / 背锅排序）；选定门派时按逐场现算。
- 选手头部：照片 + 姓名 / 编号 + 战力值 + 总场次 / 场均分 / 胜率 / 投狼率 / 站对边率 + 荣誉徽标 + 门派标签。
- 逐场战绩：按日期 / 得分排序；身份 / 门派 / 结果 / 阵营 / 标记（MVP·尽力·背锅）多维筛选；对局较多时分批加载。
- 单局复盘弹层（按人 / 按天两种视图）：
  - 花名册（狼队 / 神职 / 平民）、出局时间线与死因、违规扣分。
  - 每日技能：预言家验、女巫救 / 毒、守卫守、猎人带、摄梦、梦魇恐惧、狼美人魅惑、猎魔人狩猎、石像鬼查验、骑士决斗、侦探翻牌、怪盗狼王顶盾；狼刀按同目标合并。
  - 每日投票：按被投目标归并展示（同投一人合并成行），箭头按投狼 / 投好人着色；警徽竞选与放逐结果 / 平安白天。
  - 阵营三色（狼红 / 神蓝 / 民灰）配色。
- 深色 / 浅色主题切换，使用说明与反馈入口。
- 单文件 exe：自动从电脑版微信本地存储读取登录令牌，本地内存服务发送页面（磁盘不落 html），自动打开浏览器；令牌过期可在页面一键刷新；数据为打开时的快照。
- 受限测试版本：最多查询 20 次或运行 20 分钟。
- 错误日志：仅记录错误，写入 exe 同目录，便于反馈。

### 说明
- 登录令牌来自电脑版微信；令牌只在本机使用、不下发页面；发布物为单个 exe。
