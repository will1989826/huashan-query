# Future mobile apps

This directory reserves the boundary for a future native or cross-platform
iOS/Android app. Do not put Mini Program pages or desktop Go launch code here.

Before implementation, extract stable API field mappings and calculation test
vectors into a platform-neutral contract. Each client can then implement the
same behavior in its native language without sharing UI code.
