# 华山战力查询 Agent 开发指南

本文件是仓库级 Agent 指令的单一事实源：Codex 会自动读取根目录的
`AGENTS.md`，Claude Code 通过根目录的 `CLAUDE.md` 导入本文件。

## 适用范围

- 本文件适用于整个仓库。
- 若子目录以后需要更具体的约束，可在该目录增加更小范围的指令文件。
- 本文件收录项目特有且需要持续遵守的要求。

## 已知项目上下文

- 仓库使用 `apps/` 区分各端：`desktop/` 是 Windows / macOS 桌面版，`miniprogram/` 是微信小程序，`mobile/` 预留未来 iOS / Android 客户端。
- 桌面版支持 Windows 与 macOS；两个平台都可读取电脑版微信登录信息，并保留手动 Token 作为兜底。
- 微信小程序不连接自有服务器，Token 只保存在运行内存中，通过 `wx.request` 直接调用华山官方 API。
- 桌面版运行时仅使用 Go 1.24+ 标准库；桌面网页直接运行原生 ES Modules。
- 桌面版每个平台的发布物都是单个可执行文件；网页资源由 Go `embed.FS` 嵌入。
- 架构、目录职责、开发和发布方式以 `README.md` 为准。

## 跨端功能一致性

- 桌面版是现有功能、交互和计算口径的主要参照；为小程序或未来移动端新增功能前，先检查桌面版对应实现。
- 同一项功能在不同系统中尽可能保持入口、能力范围、字段含义、筛选排序规则、计算口径和用户文案一致。
- 平台交互方式不同时，可以采用符合该平台习惯的布局和操作，但不得无故删减核心能力或改变数据含义。
- 如果受平台接口、性能或审核规则限制而无法保持一致，应在实现和帮助内容中明确说明差异及原因。

## 日志规范

- 程序运行日志必须使用英文，包括错误信息和附加上下文。
- 日志使用准确的技术信息帮助开发者定位问题。
- 用户界面使用面向用户的独立错误文案。

## 用户文案规范

- 用户文案包括界面文字、提示、错误信息、帮助、分享文本和面向用户的说明。
- 用户文案描述用户看到的现象、得到的结果和可以采取的操作。
- 用户文案使用简短、直接、完整的表达，每句话都帮助用户理解结果或采取行动。
- 技术名词用于用户需要识别或操作该技术对象的场景。

## 狼人杀领域语言

- 牌局、身份、技能、投票和出局相关显示统一使用 `docs/standards/werewolf-language.md` 中的狼人杀术语。
- 同一概念在界面、帮助、更新说明和测试断言中使用同一个名称。
- 新增身份、版型或规则时，先依据《华山论剑选手执行手册》补充术语表，再编写显示文案。

## 帮助内容维护规范

- 每次功能迭代提交前，都必须检查程序内“使用说明”和“FAQ”是否需要同步更新。
- 功能入口、操作步骤、筛选、排序、作用域选择、结果展示或用户可执行动作发生变化时，更新“使用说明”。
- 数据来源、等待原因、加载状态、按钮置灰、缓存行为、计算口径或功能限制发生变化时，更新“FAQ”。
- 帮助内容继续遵守用户文案规范：使用说明讲清如何操作，FAQ 讲清行为原因与边界。

## 版本更新规范

- `CHANGELOG.md`、发行说明和更新提示以用户能够感知的变化为收录标准。
- 每条更新描述最终产品行为及其带给用户的价值。
- 每个版本呈现该版本最终交付给用户的变化。
- 每条更新使用一句简短、完整的用户语言。

## Git 协作规范

- 功能开发使用独立分支。
- 功能分支合入 `main` 前，先 rebase 到最新的 `main`。
- `main` 使用 `git merge --ff-only <branch>` 接收完成 rebase 的功能分支，保持提交历史线性。
